/**
 * Where the project can be supported. It is a plain link, never Ko-fi's
 * widget, so nothing loads from ko-fi.com until it is clicked — the viewer
 * stays a static client app that talks to no third party on its own.
 */
export const SUPPORT_URL = "https://ko-fi.com/el_manu";
