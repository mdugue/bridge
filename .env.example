# Copy to .env.local (gitignored) — Bun loads it for every script, Next for
# dev and build. A deployment sets SITE in its host's environment instead.
#
# The place to fetch, bake and render: an id from sites/index.ts
# (dresden, leipzig, meissen, grimma, hamburg, muenchen, berlin, unna).
SITE=dresden
